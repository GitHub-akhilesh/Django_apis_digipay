"""
Chat tools backed by the legacy DigiPay API service (`app/main.py`).

That service keeps running separately on its original URLs; these tools just let
the assistant read from it. Picked up automatically by `tools.discovery`.
"""
