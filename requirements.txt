sqlalchemy>=2.0.0
aiomysql>=0.2.0
aiosqlite>=0.19.0
redis>=5.0.0
PyJWT[crypto]>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
pydantic-settings>=2.1.0
fastapi>=0.100.0
uvicorn>=0.22.0
async-timeout>=4.0.3
exceptiongroup>=1.0.2
tomli>=2.0.0

# Agent workflow graph, used by ai_platform/workflow/graph.py.
#
# REQUIRES PYTHON >= 3.10. Every langgraph 1.x release declares it, so on 3.9
# pip resolves nothing and fails with "No matching distribution found". The
# legacy API host runs Python 3.9, which is why the AI platform there is built
# against that host's separate python3.11 — see docs/operations/deployment.md.
#
# app/services/agent_service.py also imports it, but guards the import and falls
# back to an equivalent executor, so the legacy API runs without it.
langgraph>=1.0.0
langchain-core>=1.0.0

# HTTP client used by every gateway client (gateway/client.py,
# gateway/legacy_v1/client.py).
httpx>=0.27.0

# MongoDB-backed RAG knowledge store (rag/mongo_store.py).
# The store degrades gracefully to the in-memory index if these are absent.
motor>=3.4.0
pymongo>=4.7.0
